# pie-tui

A Rust TUI with feature parity to [pi-tui](https://github.com/earendil-works/pi)
(`@earendil-works/pi-tui`) — differential rendering, synchronized output, and a
component/editor model that drops into pi-ecosystem harnesses.

Status: bootstrap. Implementation work proceeds on the `dev-rust` branch.
