# Parity ledger

Public package reference: `@earendil-works/pi-tui` 0.84.2 (canonical declarations in
`tools/api-surface.json`).
A feature may claim parity here ONLY with a link to its verifying test/script receipt.
Legend: ⬜ not started · 🟨 in progress · ✅ verified · ➖ consciously deferred (reason required)

| Tier-0 surface | Status | Evidence |
|---|---|---|
| visibleWidth / graphemeWidth / stripTerminalSequences / extractAnsiCode | ✅ | crates/pie-core/tests/golden_text.rs vs fixtures/text-golden.json (38 cases harvested live) |
| parseKey / matchesKey / isKeyRelease / isKeyRepeat / Kitty forms | ✅ for covered routes | crates/pie-core/tests/golden_keys.rs vs fixtures/keys-golden.json (105x51x2 matrix); `decodePrintable` rows remain checkpoint debt |
| wrapTextWithAnsi, truncateToWidth, sliceByColumn, getGraphemeCellRange, OSC8 helpers | ✅ | crates/pie-core/tests/golden_wrap.rs vs fixtures/text-golden.json (58 base cases × wrap/trunc/slice/columns/segments/bg/normalize matrices plus 5 embedded-SGR wrap rows) |
| StdinBuffer process/flush semantics and bracketed paste markers | ✅ | crates/pie-core/tests/golden_stdin.rs covers the pure parser/model; `adapters/pie-napi/test/m5-runtime.test.mjs` proves timer-owned idle flush and paste delivery; `tools/tmux-napi-smoke.sh` proves production lone-Escape handling and clean exit. |
| ProcessTerminal lifecycle and terminal capabilities | ✅ recorded host contract | crates/pie-term/tests/golden_process_terminal.rs and golden_terminal_capabilities.rs pin the backend/state contracts; the canonical process-stream facade is exercised by `adapters/pie-napi/test/m5-runtime.test.mjs` and both real-terminal modes in `tools/tmux-napi-smoke.sh`. |
| terminal colors and image primitives | ✅ pure/injected contracts | crates/pie-core/tests/golden_terminal_primitives.rs pins OSC color parsers, Kitty/iTerm encoders and deletes, five image formats, cell math, render/fallback/hyperlink cases, huge dimensions, and registry metadata. `calculateImageRows` matches the canonical omitted-argument literal 9x18 default but retains a JavaScript numeric/coercion gap; `imageFallback` still receives home/capability facts explicitly, and `renderImage` still receives cached capabilities and mutable global cell dimensions explicitly. The private selected Node-API package covers `allocateImageId` and global get/set cell dimensions, while the full canonical public facade remains partial. |
| TuiMainScreen differential line renderer, CSI 2026 batching | ✅ renderer scope | crates/pie-term/tests/golden_render.rs has 11 byte-equal scenarios including real width/height changes and full-clear assertions; tools/tmux-smoke.sh proves live geometry observation, stale-content clearing, and rewrap. The injected Rust screen controllers below now compose this renderer with the shared TuiBase lifecycle; production host wiring and canonical class bindings remain M5 scope. |
| Logical frame/diff and main-screen ownership seam | ✅ M4.0 | crates/pie-core/tests/frame_model.rs proves sealed normalized `LogicalFrame` construction and pure `FrameDiff`; crates/pie-term/tests/golden_render.rs preserves all 11 pinned byte/write-boundary scenarios through transactional ANSI planning and the compatibility wrapper; crates/pie-app/tests/main_screen_controller.rs proves app-owned control, stopped no-op behavior, and force handling. Forced legacy rendering while stopped resets captured state before returning without source reads or terminal writes, matching pinned 0.84.1. |
| Components wave 1: Text / TruncatedText / Spacer / Box / VStack / HStack / Loader | 🟨 partial | crates/pie-components/tests/components_golden.rs has 16 reference-harvested sequences; its oracle generator stops the timer-owning reference Loader in `finally`, so bounded regeneration exits cleanly. crates/pie-components/tests/golden_m3_components.rs adds Box cache/background/removal plus stack sizing/visibility/lifecycle and Loader indicator/lifecycle coverage. Exact Box/stack object-identity and constructor shapes plus Rust Loader runtime timer ownership remain open. |
| fuzzyMatch / fuzzyFilter | ✅ | crates/pie-core/tests/golden_m3_core.rs: 26 match and 9 filter cases from pinned compiled declarations/runtime, including UTF-16 astral vectors |
| keybinding defaults / conflicts / navigation paths | 🟨 partial | crates/pie-core/tests/golden_m3_core.rs: 5 keybinding scenarios verify scalar/list results and live identity/rebinding for retrieved shared handles. The adapter's `getKeybindings` returns a Rust-specific `SharedKeybindings`, not the canonical `KeybindingsManager`, and omits `getKeys`, `getDefinition`, `getConflicts`, and `getResolvedBindings`. Rust `KeybindingsManager` also requires vector-valued config and cannot round-trip the canonical optional constructor config with scalar/array/undefined values; exact caller-retained object identity for `setKeybindings` remains open. |
| SelectList | ✅ | crates/pie-components/tests/golden_m3_components.rs: 3 differential scenarios cover constructor state, filter, selected-index/getSelectedItem behavior, navigation/callbacks, layout/truncation bounds, and empty/newline-only description truthiness. |
| SettingsList | 🟨 partial | crates/pie-components/tests/golden_m3_components.rs: 6 differential scenarios cover grapheme editing, viewport, paste, kill/yank/undo, and submenu drainage during render/invalidate; Intl.Segmenter-style dictionary boundaries for CJK/Thai remain open, and the embedded search model is not the canonical public Input class. |
| autocomplete | 🟨 partial | crates/pie-components/tests/golden_m3_components.rs: 15 suggestion, 7 completion, and 3 trigger scenarios cover UTF-16 cursors, live fd cancellation/reaping, and exact argv/path escaping. SlashCommand verification is scoped to its exact fields plus ready and initially-pending Awaitable callback vectors. General ICU/host-locale localeCompare behavior outside the pinned Latin/ASCII corpus remains open. |
| Editor / Input | 🟨 rank-2 behavior complete, canonical rows partial | crates/pie-components/tests/editor_input_golden.rs, editor_autocomplete.rs, editor_model_surface.rs, and input_golden.rs cover rendering, UTF-16/grapheme edits, paste/history/kill/yank/undo/jump/page paths, live keybindings, callback order, and deterministic async autocomplete replacement/cancellation/Drop. The default word fallback is exact over the 30,940-case non-Thai Node 24/ICU77 partition but retains 1,439 measured Thai-adjacent divergences; host Intl injection and exact JS callback/property/NAPI behavior remain M5 seams. |
| Markdown | 🟨 rank-2 behavior complete, canonical row partial | crates/pie-components/tests/golden_m4_markdown*.rs and the checked evidence packet cover marked 18 parsing, source-position compatibility, styling/callback order, caching, indentation, UTF-8 safety, and canonical LaTeX delegation. Rust returns owned valid-UTF-8 `Vec<String>` rows, so unpaired JavaScript-string output and JavaScript Array identity remain binding-layer gaps. |
| layout / ScrollView | 🟨 rank-2 behavior + fullscreen scroll foundation verified, canonical class partial | crates/pie-components/tests/golden_layout_scroll.rs covers cached V/H layout, sizing, clipping, cursor offsets, follow/end, signed remainder, nested deepest-first targeting, primary selection, scrollbar modes, fake-clock hide/restart races, and Drop cleanup. `adapters/pie-napi/test/tier1-scroll.test.mjs` plus the exact-source `tier1-scroll-oracle.mjs` now verify production fullscreen layout boxes, always/auto scrollbar painting, deepest-first wheel routing with chain/contain remainder, overlay deferral, hover activation, thumb dragging, and stop/focus-out cleanup against authenticated 0.84.2. `selection-geometry.test.mjs` and the exact-source `selection-geometry-oracle.mjs` additionally verify ScrollView-owned/visible-clipped selection, content-row mapping after scroll, and display-grapheme endpoint snapping for wide, combining, and ZWJ text; six causal selection mutations guard that bounded tranche. Canonical ScrollView remains partial because direct-Rust timers and JavaScript child/callback identity are injected/adapted, and this bounded scroll tranche does not claim the remaining Tier-1 pane behaviors. |
| Image | 🟨 rank-2 behavior complete, canonical class partial | crates/pie-components/tests/golden_image_component.rs and tools/check-image-component-mutations.sh cover dimension priority, format detection, Kitty/iTerm/fallback selection, width caching, ID/deletion ownership including zero, numeric boundaries, default width/height limits, huge math, and 15 decisive mutations. Rust returns cloned `Vec` lines and delegates terminal deletion/host facts, so JavaScript Array/constructor/runtime identity remains open. |
| renderLatex / RenderLatexOptions | ✅ exact raw UTF-16 boundary | crates/pie-core/tests/golden_m4_latex.rs and golden_m4_latex_utf16.rs cover the pinned 66-row renderer, 108 grammar cases, exact edge/roundtrip/property matrices, the 861-row detached review corpus, and decisive mutations. `pie_napi::pi_tui::renderLatex` maps the canonical string domain to raw UTF-16 units; the scalar-friendly Rust String wrapper is deliberately strict and separately documented. |
| CancellableLoader / Container | 🟨 partial | crates/pie-components/tests/golden_m3_components.rs covers live abort signaling, Loader/Text forwarding, non-vacuous lifecycle cleanup, retained shared children, duplicate-first removal, cross-owner/stale no-op, and re-add. Real Loader timer ownership and exact JS children/object-identity surface remain open. |
| canonical public API contract | ✅ M6 baseline + bounded overlay | `tools/api-surface.json` continues to pin the authenticated 0.84.2 baseline of 133 exact symbols across 30 statements. The independent Rust ledger remains at 106 compiled mappings, 88 ported/verified, and 40 behavior-verified, with 12/30 complete statements (5 fully verified); the independent 80-symbol M3 receipt is green and the remaining 45 symbols describe direct Rust shape rather than adapter completeness. `check-surface.mjs` and `check-type-surface.mjs` distinguish baseline 69/133 + one adopted overlay = actual 70/134; no wholesale reference bump is claimed. |
| pi2dsh direct-import contract | ✅ zero deferrals | tools/pi2dsh-import-contract.json + tools/check-coverage.mjs: v0.12.4 commit `1de5a4a1` and current `2f3ced97` retain byte-identical pi-tui shim/ABI contracts; all 14 imported names now require compiled mappings and the M5 deferral allowlist is empty. |
| M5 integration | ✅ acceptance complete | The full 0.84.2 runtime/type baseline, native-backed component state, production host/screens, authenticated drift receipts, packed alias consumer, current pi-dsh front door, and Main/Alt real-tmux teardown gates are integrated. The post-M6 `setCapabilityOverrides` addition is a separately authenticated adopted overlay. |
| M6 production behavioral closure | ✅ acceptance complete | Authenticated 0.84.2 full-tree/type/semantic receipts, native Main/Alt transaction planners, terminal/TuiBase overlays and queries, Alt viewport/search/selection/image behavior, fullscreen layout regression coverage, all causal mutations, and the real packed `InteractiveMode` lifecycle pass. CI run `33302296002` proves the complete gate on `3acceec` plus runtime/types/pack/live-lifecycle receipts on macOS and Linux, arm64 and x64. |
| Shared TuiBase controller lifecycle | ✅ reviewed shared controller | `crates/pie-app/tests/fixtures/tui-controller.json` and `crates/pie-app/tests/tui_controller.rs` pin the exact 0.84.1 29-case oracle; `crates/pie-components/tests/tui_contracts.rs` proves the public rank-2 structural contracts and retained component identity; `tools/check-tui-controller-mutations.sh` contains 52 product mutations plus 33 oracle-provenance mutations. This is a screen-agnostic injected-host TuiBase foundation; reviewed injected Rust Main/Alt controllers build on it, while the production host/event loop, NAPI surface, and drop-in package remain outside this shared layer. |
| Private Node-API package | ✅ baseline 69/133 + one adopted overlay = actual 70/134 | `adapters/pie-napi` verifies authenticated 0.84.1 Rust-oracle provenance, authenticated 0.84.2 baseline behavior, and the 0.84.4 `setCapabilityOverrides` overlay against its exact tarball/source/type closure. Causal mutations cover clone, precedence, equality/cache invalidation, persistence, and provenance; clean alias install, current consumer, immutable namespace, and reproducible path-clean artifacts remain green. |
| Main/Alt TUI lifecycle | ✅ injected Rust + production facade | The pinned 0.84.1 12-case Rust oracle and its 20 product/17 provenance mutations remain green. The authenticated 0.84.2 M6 semantic oracle covers production ProcessTerminal and TuiBase/Main/Alt behavior; `tools/tmux-napi-smoke.sh` exercises the canonical JavaScript classes through input/resize/mouse/lone-Escape, alternate-buffer, and teardown. `tools/check-current-dsh-consumer.sh` proves the exact-head 0.84.2 front-door consumer plus a real fullscreen `InteractiveMode` lifecycle. |

## Accepted verification debt and ownership

- The historic M3 release target is closed: reviewed M5 foundation work raised the exhaustive
  ledger to 88 ported/verified symbols, so both the ordinary 88-symbol floor and the independent
  80-symbol receipt are green.
- Bounded residuals: general ICU locale collation, CJK/Thai dictionary segmentation,
  exact getKeybindings manager return/method surface, KeybindingsManager config shape and scalar
  round-trip, setKeybindings caller-retained identity, Box/Container public object identity and
  children shape, exact Stack constructor/types, and Loader runtime-owned timer/class wiring.
- M4.0 is complete: pure sealed frame/diff state lives in `pie-core`, terminal
  planning/emission lives in `pie-term`, and controller ownership lives in `pie-app`;
  the existing renderer fixture and real tmux smoke remain green.
- M4 feature scope is complete at the Rust compatibility layer. Editor/Input and Markdown
  remain canonical-partial only for the explicitly assigned host-Intl, JavaScript string/Array,
  callback/property, and NAPI seams; raw-UTF-16 renderLatex behavior is verified.
- M5 acceptance is complete for the 69/69 runtime and 133-name declaration baseline;
  production host/screens, current consumer, upstream-drift, and full NAPI tmux
  teardown gates are green. Preserve the lone-Escape reader timeout as a regression gate.
- M6 closes the authenticated 0.84.2 Tier-0 production paths, not full Tier-1 pane
  equivalence. The only adopted 0.84.4 addition is `setCapabilityOverrides`; the
  three `PI_*` environment overrides and all other later behavior remain outside this
  bounded overlay. A real packed 0.84.4 coding-agent probe authenticates module
  linkage, settings flow, and `InteractiveMode` construction; its full initialization
  next requires the separate later `TuiAltScreen.setCopyOnSelect` method, so it is not
  claimed by this arc. Node 24.4.1 remains the supported floor because
  the authenticated Alt search oracle diverges on the tested Node 22.19.0 runtime.
