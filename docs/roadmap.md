# Work queue & resume state

Queue is the source of truth (work-arc discipline). Reference pin:
`@earendil-works/pi-tui` 0.84.2 (`tools/api-surface.json`).

| Phase | Scope | Status | Cursor / how to resume |
|---|---|---|---|
| M0 | scaffold, boundary gate (+mutation), CI, surface manifest | ✅ f004b45 | — |
| M1a | text measurement goldens | ✅ d771ebe | — |
| M1b | keys parsing/matching goldens | ✅ 3f742ee | — |
| M1c | wrapTextWithAnsi, truncateToWidth(+truncateFragment/finalize helpers), sliceByColumn, getGraphemeCellRange, OSC8 column helpers | ✅ 5ef9323 | — |
| M2 | pie-term Terminal trait + TestRecorder; differential renderer; CSI 2026 writer; golden runner; tmux smoke | ✅ renderer scope verified | Eleven byte-equal scenarios now include real width/height changes and full-clear assertions; live tmux proves geometry observation, stale-content clearing, rewrap, and lone-Escape timeout. The shared TuiBase lifecycle and reviewed injected Rust Main/Alt controllers now compose this renderer; the production runtime and canonical class shape remain M5 scope. |
| M3 | components waves 1–2; fuzzy and keybindings; staged pi2dsh import contract; exhaustive canonical API ledger | ✅ target and receipts complete | The authenticated 0.84.2 contract brings the exhaustive receipt to 88/133 ported or verified, 40/133 behavior-verified, 106 compiled mappings, and 12/30 complete statements. Both the ordinary 88-symbol floor and `node tools/check-coverage.mjs --milestone M3` at 88/80 are green. |
| M4.0 | split pure logical frame/diff state into core, ANSI execution into term, and controller ownership into app | ✅ 8b1405a | Sealed `LogicalFrame` construction, pure `FrameDiff`, transactional ANSI plans, and app-owned `MainScreenController`; the legacy wrapper remains byte-compatible, including forced reset while stopped. |
| M4 | Editor model/component/Input + Markdown + renderLatex v1 | ✅ behavior + mapping receipt | Editor/Input lifecycle and autocomplete, marked-backed Markdown, and exact raw-UTF-16 LaTeX are integrated. Canonical rows remain partial only at the documented host-Intl, JavaScript value-identity, callback/property, and M5 NAPI seams. |
| M5 | alt-screen/layout/images/capabilities; full NAPI/TypeScript bindings and drop-in spike; event-loop/timer wiring; full tmux pack | ✅ acceptance complete | Full authenticated 0.84.2 baseline, production ProcessTerminal and canonical Main/Alt facades, 0.84.4 drift, all 14 downstream imports are enforced with zero deferrals, clean package alias, current `dsh-pi-tui-mono` front-door execution, and real-tmux teardown are green. The later bounded addition is explicit: baseline 69/133 + one adopted overlay = actual 70/134. |
| M6 | production behavioral closure for authenticated Tier-0 TuiBase/Main/Alt paths | ✅ `3acceec` + CI `33302296002` | Authenticated 0.84.2 full-tree provenance and exact 133-name types; terminal queries/overlays; native-planned Main/Alt transactions; viewport, selection, search, image, and fullscreen layout semantics; decisive mutations; real tmux; exact-head packed `InteractiveMode`; and macOS/Linux arm64/x64 receipts are green. Node 24.4.1 remains the proven floor. |
| Post-M6 overlay | adopt selected later Tier-0 additions without rebasing the M6 oracle | ✅ bounded overlay verified | `setCapabilityOverrides` alone is adopted from authenticated 0.84.4, producing actual 70/134 over the frozen 69/133 baseline. A real packed 0.84.4 coding-agent probe links the module, loads settings, and constructs `InteractiveMode`; full initialization reaches the separate later `TuiAltScreen.setCopyOnSelect` requirement, which remains outside this one-export overlay together with environment-driven capability overrides and Tier-1 pane work. |
| Tier-1 pane parity | authenticated fullscreen pane behavior beyond the closed Tier-0 envelope | 🟨 scroll + bounded selection-geometry tranches verified | Exact 0.84.2 source hashes and public-behavior receipts cover ScrollView box/hit metadata, always/auto scrollbar paint, deepest-first nested wheel routing with chain/contain remainder, capturing-overlay deferral, hover activation, thumb dragging, stop/focus-out cleanup, ScrollView-owned/visible-clipped selection, content-row mapping after scroll, and display-grapheme endpoint snapping for wide/combining/ZWJ text. Eighteen causal mutations across the scroll and selection tranches plus the packed/current-consumer ladders guard this bounded foundation; word/double-click, copy transport/OSC52, auto-scroll timers, search/flash semantics, the 0.84.4 `copyOnSelect` addition, and the rest of Tier-1 remain outside this tranche. |

Verification ladder per `AGENTS.md`. The normal coverage gate protects the proved
88-symbol floor; the explicit M3 milestone receipt is independently green, while M6
is closed by its authenticated semantic/mutation, live-consumer, and four-target CI gates.
